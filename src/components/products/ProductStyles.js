import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
    root: {
        maxWidth: 280
    },
    media: {
        height: 230
    },
    gridBox: {
        margin: "auto"
    }
}))
